// const compression = require("compression");
// const zlib = require("zlib");

module.exports = function (app) {
    // app.use(compression({ level : zlib.Z_BEST_COMPRESSION, chunkSize : 10000 }));
};
